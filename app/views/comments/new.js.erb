// Javascript to be executed
Modalbox.show('<%= escape_javascript(render 'new', :commentable => @commentable) %>', {width: 400, transitions: false, overlayClose:true, overlayOpacity: 0.0});

add_hide_observer();
