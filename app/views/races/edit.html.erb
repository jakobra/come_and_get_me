<% title @race.training.date.strftime("%e %b - %Y") %>
<% main_class "large_width" %>

<h1>Edit Race</h1>

<% form_for @race do |f| %>
	<%= render "fields", :f => f %>
	
	<p class="clear_left">
		<%= f.submit 'Update' %>
	</p>
<% end %>

<%= link_to "Back", training_path(@race.training) %>