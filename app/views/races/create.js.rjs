page << "if ($('errorExplanation') != undefined) {"
  page.remove :errorExplanation
page << "}"

flash.discard

unless @race.new_record?
  page.insert_html :bottom, :races, :partial => @race, :locals => {:authorized => authorized?(@training)}
  page.replace_html :race_form, :partial => "trainings/new_race_form", :locals => {:race => @training.races.build}
  
else
  page.replace_html :race_form, :partial => "trainings/new_race_form", :locals => {:race => @race}
end



